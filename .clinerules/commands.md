# Commands

Always-on rules for this repository.

## Shell

- Use **Git Bash** for all shell commands in this repo, not PowerShell/CMD. Quoting, env vars, and line continuations behave predictably there; Windows-native shells introduce avoidable breakage.
